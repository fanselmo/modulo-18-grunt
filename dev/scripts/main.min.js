document.addEventListener("DOMContentLoaded",function(){document.getElementById("form-sorteador").addEventListener("submit",function(evento){evento.preventDefault();evento=document.getElementById("numero-maximo").value,evento=parseInt(evento),evento=Math.random()*evento;Math.floor(1+evento)})});